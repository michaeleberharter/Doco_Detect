# STILLGELEGT: der MobileSAM-Pfad ist nicht mehr in die Pipeline eingebunden
# (der globale Graph-Cut in segmentation.py hat ihn ersetzt). Diese Abhaengig-
# keiten braucht nur noch, wer mit docodetect/neural_seg.py experimentiert.
# CPU reicht; der ~40-MB-Checkpoint laedt beim ersten Lauf nach models/.
torch>=2.2
torchvision>=0.17
timm>=0.9
segment-anything-hq>=0.3
mobile-sam @ git+https://github.com/ChaoningZhang/MobileSAM.git
